{
  "name": "cactuspi-client",
  "version": "0.2.0",
  "description": "A raspberry pi client listening on a server to receive message to display on a LED matrix board.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Danny Lum",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.966.0",
    "mqtt": "^4.2.8",
    "pubnub": "^4.27.3",
    "queue": "^6.0.1"
  }
}
